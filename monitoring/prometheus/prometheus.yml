global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/zkvote-alerts.yml

scrape_configs:
  - job_name: zkvote-relayer
    metrics_path: /metrics
    static_configs:
      - targets: ["backend:8080"]
